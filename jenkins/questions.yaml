questions:
  - variable: persistence.storageClass
    default: "-"
    description: "If undefined or null, uses the default StorageClass. Default to null"
    type: storageclass
    label: Default StorageClass for jenkins
  - variable: master.ingress.enabled
    default: false
    description: "Enables ingress"
    label: Use Default Image
    type: boolean
  - variable: master.ingress.hostName
    default: 	{}
    description: "Ingress host name"
    label: 	Ingress host name
    type: string
  - variable: master.ingress.annotations
    default: 	{}
    description: "Ingress host name"
    label: 	Ingress host name
    type: string


